# PacMap on Windows

Windows capture requires Npcap. Install Node.js 22+ and Python 3.12+.

Create a virtual environment, install `requirements.txt`, install the client dependencies with `npm install`, then start `server.py` with the Npcap interface selected for capture.
